# Resume and Application Assets

## Project Title

**Reproducible End-to-End ML Workflow | Python, SQL, scikit-learn, CI/CD**

## English Resume Bullets

- Built a reproducible end-to-end ML workflow for four-class STEM text routing across {{ claims.source_rows }} pinned records, covering schema validation, duplicate-aware grouped splitting, SQLite analytics, hyperparameter tuning, error analysis, and automated reporting.
- Evaluated {{ claims.parameter_configurations }} TF-IDF and linear-model parameter configurations using leakage-safe five-fold grouped cross-validation; the locked {{ claims.model_name }} achieved {{ claims.macro_f1 }} macro-F1 and {{ claims.accuracy }} accuracy on {{ claims.test_rows }} isolated test records, with a 95% bootstrap interval of {{ claims.ci_lower }}-{{ claims.ci_upper }}.
- Implemented {{ claims.sql_query_count }} named SQL analyses, artifact hashing, one-time test evaluation, automated tests, clean-room verification, GitHub Actions, and a visually audited {{ claims.report_pdf_pages }}-page technical report with zero unresolved numerical discrepancies.

## 中文履歷敘述

- 建立可重現的端到端機器學習流程，處理 {{ claims.source_rows }} 筆固定版本資料，涵蓋 schema 驗證、重複群組隔離切分、SQLite 分析、超參數調整、錯誤診斷與自動化報告。
- 以五折 grouped cross-validation 比較 {{ claims.parameter_configurations }} 組 TF-IDF 與線性模型設定；鎖定的 {{ claims.model_name }} 在 {{ claims.test_rows }} 筆獨立測試資料達到 macro-F1 {{ claims.macro_f1 }}、accuracy {{ claims.accuracy }}，95% bootstrap 區間為 {{ claims.ci_lower }}-{{ claims.ci_upper }}。
- 實作 {{ claims.sql_query_count }} 個具決策目的的 SQL 查詢、artifact hash、一次性測試評估、自動測試、clean-room 驗證、GitHub Actions 與逐頁 QA 的 {{ claims.report_pdf_pages }} 頁技術報告。

## ML / Data Science Application Variant

Emphasize leakage-safe experimental design, grouped CV, macro-F1, bootstrap uncertainty, per-class errors, feature interpretation, and the decision not to claim calibrated confidence.

## IMC / Intelligent Manufacturing Application Variant

Emphasize the transferable workflow: controlled data ingestion, automated quality checks, high-dimensional feature pipelines, reproducible tuning, failure-slice diagnosis, SQL decision support, and lifecycle verification. Do not describe the benchmark itself as manufacturing validation.

## STAR Interview Outline

- **Situation:** A public ML portfolio needed to demonstrate trustworthy workflow quality rather than a frontend demo.
- **Task:** Determine whether question text alone could support reliable four-subject routing without contaminating the final test.
- **Action:** Pinned the source revision; validated, normalized, and deduplicated records; enforced grouped splits; tuned pipelines with grouped CV; locked the selection; evaluated once; diagnosed class, source, and length failures; automated evidence generation and QA.
- **Result:** The locked model reached macro-F1 {{ claims.macro_f1 }} with a {{ claims.ci_lower }}-{{ claims.ci_upper }} interval, while the project explicitly documented source confounding, uncalibrated margins, and the next valid experiments.
